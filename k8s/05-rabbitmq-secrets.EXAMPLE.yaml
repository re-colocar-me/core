# Template só pra documentar as chaves necessárias — NUNCA aplicar este arquivo como está
# nem commitar a versão real preenchida. O Secret de verdade é criado via
# `kubectl create secret generic rabbitmq-secrets -n consultor --from-literal=...`.
apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-secrets
  namespace: consultor
type: Opaque
stringData:
  username: "TROCAR"
  password: "TROCAR"
