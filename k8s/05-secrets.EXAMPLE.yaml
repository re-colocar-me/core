# Template só pra documentar as chaves necessárias — NUNCA aplicar este arquivo como está
# (valores fake) nem commitar a versão real preenchida. O Secret de verdade é criado via
# `kubectl create secret generic core-secrets -n consultor --from-literal=...`, nunca a partir
# deste arquivo.
apiVersion: v1
kind: Secret
metadata:
  name: core-secrets
  namespace: consultor
type: Opaque
stringData:
  connection-string: "TROCAR — Host=postgres.consultor.svc.cluster.local;Port=5432;Database=ReColocarMe;Username=postgres;Password=...;"
  jwt-secret: "TROCAR — min 32 chars"
  linkedin-secret: "TROCAR — LinkedIn app client secret"
  hermes-agent-api-key: "TROCAR — mesmo valor do API_SERVER_KEY do Hermes (bot-hermes/hermes-k3s)"
